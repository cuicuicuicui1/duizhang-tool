# 设计决策记录

> 记录执行过程中**对原计划（`PLAN_往来单位对账函工具.md`）的判断、偏离与补充**，
> 以及无法自行验证、需要会计或你确认的事项。
> 标注约定：`DECISION`＝执行时拍板；`DEVIATION`＝与原计划不一致；`UNVERIFIED`＝无法在本机验证；
> `MANUAL_REVIEW`＝业务判断，已做成界面上的可选项或提示。

---

## 一、按你的指示调整的设计

### D1 `DECISION` 余额口径不写死，全部做成可选项

原计划假定执行者拍板一种口径（§5.2 用"己方期初+借−贷"）。你要求"这些加上你自己想到的，让用户自主选择"，因此改成**六种可选策略**，由会计在『设置』页切换：

| 值 | 说明 |
|---|---|
| `opening_from_file` | 用导入识别到的期初行/列 |
| `cumulative` | 直接累计借贷（期初视为 0） |
| `manual_opening` | 期初由会计在单位档案里手工录入 |
| `balance_sheet` | 以科目余额表的期末余额列为准 |
| `incremental` | 分次导入累加（期初表 + 期间明细） |
| `auto`（默认） | 先找期初，找不到回退累计并在函上提示口径 |

实现为 `src/ledger.js` 的 `BALANCE_STRATEGIES` 注册表（每个策略是纯函数，可单测）。未知口径值会回退到累计法并给出可读告警，不会崩。

**我补充的第 5、6 种**：`incremental`（数据分批导出补录的真实场景）和 `auto`（不强迫会计先做选择，但会在函上提示"口径待确认"，而不是静默给一个数）。

### D2 `DECISION` 应付/余额方向、抵销与否也做成可选项

- `signConvention`：按科目自带方向（应收＝借−贷、应付＝贷−借，负数视为方向异常）／一律存正数方向另记。
- `netting`：默认**分别列示不抵销**，并在函尾打 MANUAL_REVIEW 提示人工确认是否净额结算。
- 同一单位的多个科目（应收账款＋预收账款）在函上分栏列示，各自带余额与大写。

### D2b `DECISION` 科目自然方向字典也做成可配

原计划把它写死在代码里（正则判断）。问题是：**只有「单列金额且没有方向列」的明细账才用得上它**，
而各家的科目命名各不相同（「内部往来」「往来款-XX」「备用金」这类中性名称根本判不出方向）。
既然口径都能让用户选，这个也不该例外：改成设置页可编辑的两张关键词清单（借方 / 贷方），
判不准的一律回退到向导里指定的方向并在预览页标注。

顺带修掉一个内置清单的潜在误判：原来的正则里放了「长期」，
导致「长期借款」同时命中借（长期）与贷（长期借款）两边而被判为"不确定"。新清单去掉了这类含糊词，
并明确把「长期借款 / 累计折旧 / 坏账准备 / 递延收益」放到贷方，「长期股权投资 / 长期应收款 / 累计摊销」放到借方。

### D2c `DECISION` 列映射模板库：对「格式碎片化」的开源式回答

- 背景讨论：用户质疑「财务软件导出格式各不相同 → 更适合做针对性工作 → 项目没可行性」。
- 回应：工具适配的单位是**文件形态**（约 6~7 种）而非软件品牌；认不出的格式走一次性手工映射（30 秒），之后按文件记忆。
- 本节落地的新设施：**列映射模板库**（`src/mapTemplates.js`）——
  模板 = 名字 + 软件名 + 表头特征（归一化签名）+ 列映射；同表头文件自动建议一键套用；
  导出/导入 JSON 文件 = 社区分享载体；同签名同映射去重；使用计数排序。
- 红线不变：模板只做「建议」，套用由用户点击，套用后仍走预览/余额校验/人工核对全流程。
- 测试：t2 新增 18 条断言（含归一化匹配、去重、坏条目跳过）。

### D3 `DECISION` 回函格式不统一 → 四层降级 + 格式记忆（你点名要我想的部分）

回函格式我们控制不了，所以 `src/replyAdapter.js` 做了四层：

1. **自动识别**：复用 `columns.js` 别名字典（对方写"发生额""往来款"，在设置页把它加进字典即可）；
2. **格式记忆**：把"这家单位的回函长什么样"存成方案（`data/replyMaps.json`），**按单位**记忆，下次同一单位回函自动套用；方案还能导出成 `reply-maps.json` 在会计之间共享；
3. **手工映射 / 极简模式**：表头认不出就手工点表头行与列；只有一列数字也认（正负号定方向）；
4. **粘贴模式**：从任意表格复制几行粘进来，自动嗅探 Tab/逗号/分号/竖线/多空格分隔；没表头时按列内容特征猜（日期列/数字列/文本列/方向列）；
5. **兜底**：只剩一个余额 → 走"仅余额"路径，报告标 MANUAL_REVIEW。

### D4 `DECISION` 方向翻转只作用于明细，不作用于余额

原计划没有这项。实际业务里，对方常把自己账套导出的明细账直接发过来，**他们账上的贷方＝我方账上的借方**。

- `signFlip` 的语义定为：**只把对方明细的借贷两列对调**。
- **余额不翻转**：对账函上对方填的余额就是"对方欠我方多少"（应付场景为"我方欠对方多少"），与我的账面同向，照抄即可。翻转余额会把正确的数变成错的（这点我在测试里验证过：翻转余额会让"丙"的余额差从 0 变成 40,000.00）。
- **自动建议**：勾对时额外跑一遍"翻转后"的算法，若待处理项明显减少，就在界面上提示"疑似对方明细方向相反，建议翻转后重跑"，并给出翻转后的项数。

---

## 二、与原计划不一致的地方（`DEVIATION`）

### V1 PDF 引擎：三条通道并存，默认「复用单个浏览器实例」

- 计划 §1 指定 `puppeteer-core` + 探测系统浏览器（"禁止下载 Chromium"）。这条没变：三个通道用的都是**系统 Edge 的内核、同一份 HTML、同一套 `@page` 打印样式**，产物等价（都验过 A4 MediaBox + 中文 ToUnicode），全程不下载 Chromium。
- 实测三条通道：

  | 通道 | 单份耗时 | 50 家 × 200 行批量 | 依赖 |
  |---|---|---|---|
  | **共享单实例**（`createPdfSession`，默认） | **约 0.24 秒** | **约 45 秒**（含 Excel 与 200 行明细附页渲染） | puppeteer-core 可加载 |
  | 浏览器命令行 `--print-to-pdf`（兼容模式） | 约 1.1 秒 | 约 55 秒（串行 4 profile 轮换） | 无（只用系统浏览器） |
  | 每次新起 puppeteer 实例 | 约 6.1 秒 | 10 路并发下**卡住不返回**（弃用） | 同上 |

- 决策：默认 `pdfEngine = 'auto'`——先尝试开一个**复用的浏览器实例**顺序渲染所有函件（快 4 倍且不受并发争抢影响）；puppeteer-core 装不上或浏览器启动失败时**自动回退**到命令行通道，并在批次结果里给出警告。两条通道可在设置页切换。
- 教训记录：早前"puppeteer 卡死"的根因不是 puppeteer 本身，而是**10 个实例并发启动**争抢浏览器。单实例顺序渲染反而既快又稳。
- 另有 `tools/fix-deps.js`：扫描 `node_modules` 找出"入口声明的目录存在但文件缺失"的包，从 registry 重新下载 tarball 手工解包补齐（一次修复了 `proxy-agent`/`http-proxy-agent`/`pac-*`/`socks-proxy-agent`/`netmask` 共 5 个真实损坏的包）。

### V1a 无头 Edge 的进程泄漏：`browser.close()` 杀不干净，必须"先记树、后强杀"

全量回归第一次卡死 17 分钟，根因不在测试，在浏览器生命周期：

- 现象：`browser.close()` 之后 Edge 主进程退了，但 crashpad/GPU/网络服务这些**子进程被孤儿化**留在系统里。每跑一批生成漏约 3 个进程，反复启动后系统里堆了 30 个 msedge.exe，某次 `launch()` 直接挂死。
- 误区：`taskkill /T /PID <主进程>` 在主进程已死后无效——`/T` 靠父进程关系找子进程，孤儿化后关系断了。
- 正确姿势（`exporter.createPdfSession` 已实现）：
  1. **close 之前**用 `Get-CimInstance Win32_Process -Filter "ParentProcessId=<pid>"` 递归记下整棵进程树（此时树还完整）；
  2. 优雅关闭（最多等 8 秒）；
  3. 对树里每个 pid `taskkill /F` 补刀；
  4. launch 加 20 秒超时、`print` 里 `page.close()` 放 finally、并加 `--disable-crashpad` 等参数减少常驻辅助进程。
- 验证方法：连续 5 个批次前后对比 `msedge` 进程数。修复前 13 → 28，修复后 13 → 13（零泄漏）。

### V1c `process.exit()` 在本机偶发挂住：编排器看门狗 + 从输出判定结果

- 现象：套件把全部断言跑完、结果打印完之后，进程偶尔不退场（HTTP 服务类套件 t6/t7 更常见，t3/t5 也出现过）。
  连 `finish()` 里的 `process.exit()` 都拦不住——进程带着已完成的输出僵在那里，让 `spawnSync` 干等十几分钟。
- 处置（`run_all.js` + `_harness.js`）：
  1. harness 在 FORCE_EXIT 模式下先设 `process.exitCode`，再挂一个 3 秒定时器重试退出（双保险）；
  2. run_all 改为**管道捕获子进程输出 + 6 分钟硬超时看门狗**，超时强杀后继续；
  3. 通过与否**不依赖退出码**，从输出里的「失败 0」判定——强杀后的非零退出码不会把全过的套件误判为失败。
- 实测：8 套件 735 条断言全绿；t3/t5 被看门狗强杀但判定正确，全程 32 分钟（其中约 30 分钟是两次 15 分钟版看门狗等待，收紧到 6 分钟后正常全量约 6–7 分钟）。
- 对产品无影响：真实使用是常驻 server 进程，不存在"跑完要退出"的场景。

### V1b 批量并发数实测（仅影响兼容模式）

共享单实例是顺序渲染，不需要并发；并发数只作用于**回退到命令行通道**时。同一台机器、12 家 × 200 行，命令行通道只改并发数：

| 并发 | 耗时 |
|---|---|
| 1 | 20.4 秒 |
| 2 | 11.6 秒 |
| **3–4** | **10.2 秒** |
| 6 | 11.5 秒 |
| 10 | 26.1 秒（并发争抢，反而更慢） |

默认取 4（可在设置里改）。两个必须点：**必须用异步 `execFile`**（`execFileSync` 会阻塞事件循环，让限流池形同虚设）、**每次调用带独立 `--user-data-dir`**（共用默认 profile 时多个实例互相抢锁，并发等于串行）。

### V2 备份目录放在 `backups/`（项目根），不放在 `data/` 内

计划 §4.3 写的是 `BK=data/_backup_时间戳; cp -r data $BK`。这样执行会把**历次备份再套娃复制**（备份里含备份），体积指数增长。改为项目根的 `backups/{时间戳}_{原因}/data/*.json`。
自动备份只复制 `*.json`（数据本体，通常 < 2MB）；原件、PDF、归档由『整包备份（zip）』负责。

### V3 临时渲染文件按"槽位"固定文件名

红线要求程序内不出现删除动作。若按时间戳生成临时 HTML，批量生成 50 家就会在 `data/tmp` 留下几十上百个永不清理的文件。改为 `data/tmp/render_slot{N}.html` 覆盖写（N 与 PDF 并发槽位一致），不会再堆积。

### V4 L1/L2 的判定条件

计划 §5.2 把 L1 定为"金额相等一对一贪心"，L2 定为"金额相等 + 日期差 ≤ 15 天"，两者按字面含义重叠（L1 会把 L2 的候选吃光）。按业务意图拆开：

- **L1 完全一致**：金额（按我方方向折算后的带符号分）相等 且 日期相同或相差 ≤ 3 天
- **L2 时间性差异**：金额相等 且 3 < 日期差 ≤ 窗口（默认 15 天，可配）
- 额外增加一档：金额相同但日期差**超出窗口** → 归入"可疑记录"而非直接匹配掉

### V5 组合匹配按绝对值同号匹配

计划只说"跑子集和"。实测发现若要求金额为正，则"我方 5 笔借方 ↔ 对方 1 笔贷方（对方口径）"这种最常见的汇总场景匹配不到。改为：按**同号 + 绝对值为目标**做子集和，上限 8 项、候选 ≤ 20 笔、带步数预算（20 万步）；超出上限转 MANUAL_REVIEW。方向 A（我方多笔＝对方一笔）与方向 B（对方多笔＝我方一笔）都跑。

### V6 其余补充（原计划未覆盖，均为实现时必需的细节）

| 项 | 处理 |
|---|---|
| 期初余额存哪 | 新增 `data/openings.json`（`source: file/manual`），而非塞进 ledgers |
| 科目余额表怎么入库 | 无日期行照常入库（`date: null`），余额存 `balanceFen`；本期发生额计入这些无日期行 |
| 同一文件多科目 | 科目列里是往来科目时**按行取科目**，同一单位自动拆成应收/应付两个余额分别列示 |
| 编号作用域 | 序号按"当日全局自增"（`counters.json`），避免同一截止日两个批次都从 0001 起而重号 |
| 归档文件名 | 加编号前缀（`DZH-20260831-0001_对账函.pdf`），规避 Windows + zip + 浏览器下载链路的中文编码坑 |
| 文件上传 | 走 JSON + base64（不引入 multer），避免多一个依赖，也方便 jsdom 测试 |
| 明细附页 | 按科目分组渲染，各自滚动余额；模板与 Excel 导出共用 `ledger.detailRows()`，保证两条出口一致 |
| 导入预览可核对性 | 预览表格新增「解析为借方 / 解析为贷方」两列 —— 冲销/红字/方向判断这三类风险都能在这一页肉眼逐行核对 |
| 批量生成性能 | 编号/版本顺序分配，渲染+导出按槽位并发（默认 3，实测最快值），`ledgers.json` 读取带 mtime+写钩子双保险缓存 |

---

## 三、无法在本机验证的事项（`UNVERIFIED`）

1. **红字负数**：财务软件里"红字"有时只靠单元格颜色（数字本身是正的）。Excel 颜色在本工具里读不到，因此红字若不以括号/负号形式出现，会被当成正数。已在导入预览页提示人工核对，报告里也注明该限制。**建议：用你真实的导出文件试一次，确认红字是以括号还是负号呈现。**
2. **科目自然方向字典**：现已做成设置页可编辑的关键词清单。但**默认清单是否覆盖你单位的科目命名，需要你拿真实文件核一次**——
   判断方法：导入预览页新增了「解析为借方 / 解析为贷方」两列，拿已知方向的流水对照即可。
   只有「单列金额且无方向列」的文件才涉及这一项；双列借贷或带方向列的文件不受影响。
3. **模板是否符合你单位的对外函件规范**：函件措辞、回函联格式、是否需要骑缝章提示，工具给的是通用版式，是否符合你单位/审计/税务口径需要你确认（模板可在界面上直接改）。
4. **PDF 打印机型差异**：本机验证的是 Edge 打印。若会计机器只有 Chrome，代码里已按 Edge → Chrome 顺序探测，但**未在纯 Chrome 环境实测**。

## 四、已做成界面可选项/提示的判断事项（`MANUAL_REVIEW`）

- 余额口径未取到期初 → 函上提示"口径待确认"，不静默给数
- 余额方向与科目常规方向相反 → 函上标"余额方向异常"
- 同一单位多个科目余额 → 函上分栏列示，并提示是否净额结算
- 单位仅有余额、无逐笔明细 → 提示"无法逐笔核对，建议索取明细"
- 勾对时有未匹配项 → 差异报告区块列出全部未匹配项，确认按钮永远在会计手里
- 组合匹配候选超过 20 笔 → 跳过并提示"请人工勾对"
- 单列金额且未指定科目方向 → 导入预览页高亮提示
- 金额解析遇到 `NaN`/`#REF!`/超出安全整数范围 → **报错而不是当 0 静默入库**
- 疑似万元/千元单位 → 明确报错，不自动换算

---

## 五、测试与验收实测数据

| 层 | 套件 | 断言数 | 关键实测 |
|---|---|---|---|
| 1 主场景端到端 | `t8_e2e.js` | 82 | 建 3 家 → 导入 13 条 → 出函 3 份 → 录回函 → 勾对 → 归档，差异表与预期完全一致 |
| 1/4 生成与导出 | `t3_statement.js` | 90 | 50 家 × 200 行 **45.0 秒**（共享单实例顺序渲染，达标线 60 秒）；PDF 为 A4 且含中文 ToUnicode；留痕行齐全 |
| 1/3 导入解析 | `t2_import.js` | 158 | 18 类脏数据样本（.xls/GBK/BOM/千分位/括号负数/万元/全角/多 sheet/余额表/期初行）全过 |
| 1/2 勾对算法 | `t4_matcher.js` | 106 | 一致/未达账项/组合匹配/方向翻转/方向记反/疑似重复/超窗口日期/仅余额 全部场景 |
| 2 内核单测 | `t1_money.js` | 89 | 金额脏格式 + 大写；1000 次随机往返一致性 |
| 3 边界脏数据 | `t5_edge.js` | 60 | 空文件/假 Excel/乱码二进制/NaN/Infinity/超安全整数/**5 万行（导入 3.3 秒、余额 375ms、出函 46 秒）** |
| 5 运维 | `t6_ops.js` | 67 | 备份→改数据→恢复往返一致；幂等；版本递增；删除保护；路径穿越校验 |
| 6 前端 jsdom | `t7_frontend.js` | 101 | 8 个 Tab 逐个渲染 + 真实交互；无 window.error、无非预期 console.error、页面无 `undefined`/`NaN` |

合计 **753** 条断言，`node tools/rdtest/run_all.js` 一把跑完（8 个套件，全部通过；正常全量约 6–7 分钟，若个别套件进程退出挂住会被看门狗强杀，判定不受影响——见 V1c）。
快速模式 `--quick` 跳过 50 家规模用例，约 90 秒。

---

## 六、后续可以考虑的增强（未实现，供你判断）

- 电子签章 / 邮件群发（原计划明确 out of scope，仍不建议做）
- 对方扫描件 OCR 识别回函（原计划二期再议）
- 对账函"待回函 / 已催函 / 已回函"的状态看板与催函清单
- 跨期间趋势：同一单位近几个月的余额与差异走势
- 把差异原因（对方漏记/我方漏记/在途）沉淀成结论文本，直接写进差异报告存档
